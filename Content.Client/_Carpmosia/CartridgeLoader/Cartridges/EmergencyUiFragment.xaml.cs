using Content.Client.Message;
using Content.Client.Stylesheets;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;
using Robust.Shared.Utility;

namespace Content.Client._Carpmosia.CartridgeLoader.Cartridges;

[GenerateTypedNameReferences]
public sealed partial class EmergencyUiFragment : BoxContainer
{
    public event Action<string>? OnEmergencyMessage;

    public EmergencyUiFragment()
    {/*
        RobustXamlLoader.Load(this);
        Orientation = LayoutOrientation.Vertical;
        HorizontalExpand = true;
        VerticalExpand = true;

        UpdateState(new List<string>());
    */}

    public void UpdateState(BoundUserInterfaceState state)
    {
        DelayLabel.SetMarkup(Loc.GetString("emergency-cartridge-ui-timer",
                ("time", 1)));
    }
}
